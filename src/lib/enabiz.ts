export async function sendApprovedReportToENabiz(_reportId: string) {
  // e-Nabiz integration placeholder: real Ministry of Health API client can replace this mock.
  return {
    ok: true,
    mockTransactionId: `MOCK-ENABIZ-${Date.now()}`
  };
}
